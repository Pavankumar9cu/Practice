import logo from './logo.svg';
import './App.css';

function App() {
  return (
    <div className="App">
     <h1>Local Repository : </h1>
     <p>To pull from the remote Repository</p>
     <h1>Remote Repository :</h1>
     <p>To push from the Local repository </p>
     <h1>Push :</h1>
     <p>To add the changes from remote repository by using push</p>
     <h1>Pull :</h1>
     <p>To get the latest upades from remote repository by using pull.</p>
    </div>
  );
}

export default App;
